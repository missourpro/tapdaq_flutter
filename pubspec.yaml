name: tapdaq_flutter
description: Tapdaq Flutter plugin mediation platform.
version: 1.0.0
environment:
  sdk: ">=2.1.0 <3.0.0"
  flutter: ">=1.10.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.solion.tapdaqflutter
        pluginClass: TapdaqFlutterPlugin
